node_modules/
dist/
.env
.env.local
*.log

# Next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# Fontes baixadas e artefatos de ingestão (recriáveis via npm run ingest:download)
data/sources/
data/build/

.vercel

# Supabase CLI (temporário, não versionar)
supabase/.temp/

# Versões licenciadas (texto protegido por direitos autorais NUNCA é versionado;
# o exemplo de formato é a única exceção permitida)
data/versions/*
!data/versions/_example.json
